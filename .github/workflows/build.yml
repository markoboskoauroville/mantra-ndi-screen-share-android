name: Build

# android-app.md §1a: the APK is built by GitHub Actions and NEVER on a desk.
# Push, wait about five minutes, and the APK is a release on this repository —
# downloadable without a token, so the link can be opened on the phone.

on:
  push:
  workflow_dispatch:

permissions:
  contents: write   # the workflow publishes the release itself

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read the version
        id: version
        run: |
          V=$(grep -oP '^appVersion=\K\d+' gradle.properties)
          echo "v=$V" >> $GITHUB_OUTPUT
          echo "Building v$V"

      - name: Gates over the source
        run: python3 scripts/verify.py

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # android-app.md §3: one permanent key for the life of the app. A
      # different key is a different app to Android, and every install after it
      # is an uninstall first.
      - name: Restore the signing key
        env:
          KEYSTORE_B64: ${{ secrets.SIGNING_KEYSTORE_B64 }}
        run: |
          if [ -z "$KEYSTORE_B64" ]; then
            echo "::error::No signing key. An unsigned or differently signed APK cannot update an install."
            exit 1
          fi
          mkdir -p signing
          echo "$KEYSTORE_B64" | base64 -d > signing/mantra-ndi-screen-share.p12
          echo "Key restored, $(stat -c%s signing/mantra-ndi-screen-share.p12) bytes"

      # ─────────────────────────────────────────────────────────────────────
      # The SDK is licensed and confidential, so it lives in its own private
      # repository and is fetched here. It is never committed to this one.
      #
      # A READ-ONLY DEPLOY KEY, NOT A TOKEN, and the difference matters because
      # this repository is PUBLIC. A personal access token carries whatever its
      # owner can reach — on this account, push rights across every repository —
      # and it would be sitting in the secret store of a public repo's CI. A
      # deploy key is one keypair, on one repository, read-only, revocable by
      # itself and useful for nothing else. If it leaks, the worst case is that
      # somebody can read the SDK; with a token the worst case is the account.
      #
      # `gh repo deploy-key add` is read-only unless --allow-write is passed.
      # It was not passed.
      # ─────────────────────────────────────────────────────────────────────
      - name: Fetch the NDI Advanced SDK (read-only deploy key)
        env:
          SDK_DEPLOY_KEY: ${{ secrets.NDI_SDK_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$SDK_DEPLOY_KEY" ]; then
            echo "::error::No NDI SDK deploy key, this would build an app that cannot send."
            exit 1
          fi
          install -m 700 -d ~/.ssh
          printf '%s\n' "$SDK_DEPLOY_KEY" > ~/.ssh/ndi_sdk
          chmod 600 ~/.ssh/ndi_sdk

          # THE HOST KEY IS FETCHED FROM GITHUB RATHER THAN TRUSTED ON SIGHT.
          #
          # `ssh-keyscan github.com` is trust-on-first-use: it believes whatever
          # answers, which on a compromised runner network is the wrong thing.
          # Writing the key into this file instead would be authenticated, and
          # would also be a copy that silently rots the day GitHub rotates it —
          # and a rotation would then read as a build failure nobody can explain.
          #
          # api.github.com/meta publishes the current keys, and curl validates
          # the TLS certificate before we read a byte. Authenticated AND current.
          curl -fsSL https://api.github.com/meta \
            | python3 -c "import sys,json;[print('github.com',k) for k in json.load(sys.stdin)['ssh_keys']]" \
            >> ~/.ssh/known_hosts

          # NO 2>/dev/null ON THE CLONE. It was suppressed before, so a refused
          # key produced no message here and the build died at the `cp` below
          # with "no such file" — which reads as a missing SDK rather than as a
          # credential that was turned away.
          GIT_SSH_COMMAND="ssh -i ~/.ssh/ndi_sdk -o IdentitiesOnly=yes" \
            git clone --depth 1 git@github.com:markoboskoauroville/mantra-ndi-sdk.git /tmp/ndi-sdk
          rm -f ~/.ssh/ndi_sdk

          # This path is not free to change: app/build.gradle.kts decides whether
          # to build the native code at all by testing for
          # src/main/cpp/ndi/include/Processing.NDI.Lib.h. Clone somewhere else
          # and the NDK step is skipped silently, and the artefact gate then
          # fails with "bridge missing" — a path problem wearing an SDK problem's
          # clothes.
          mkdir -p app/src/main/cpp/ndi/include app/src/main/jniLibs/arm64-v8a
          cp /tmp/ndi-sdk/include/*.h app/src/main/cpp/ndi/include/
          cp /tmp/ndi-sdk/lib/arm64-v8a/libndi_advanced.so app/src/main/jniLibs/arm64-v8a/
          rm -rf /tmp/ndi-sdk
          test -f app/src/main/cpp/ndi/include/Processing.NDI.Lib.h
          test -f app/src/main/jniLibs/arm64-v8a/libndi_advanced.so
          echo "PASS  SDK in place at the path Gradle tests for"

      - name: Accept SDK licences
        run: |
          SDKMANAGER=$(ls -d "$ANDROID_HOME"/cmdline-tools/*/bin/sdkmanager | head -1)
          yes | "$SDKMANAGER" --licenses > /dev/null 2>&1 || true

      - name: Install NDK and CMake
        run: |
          SDKMANAGER=$(ls -d "$ANDROID_HOME"/cmdline-tools/*/bin/sdkmanager | head -1)
          "$SDKMANAGER" --install "ndk;27.0.12077973" "cmake;3.22.1"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      # Test 1 of the four: the mechanism, alone.
      - name: Test 1, the mechanism
        env:
          SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
        run: gradle testDebugUnitTest --stacktrace

      - name: Assemble signed APK
        env:
          SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
        run: gradle assembleDebug --stacktrace

      # Gates over the artefact the person will actually receive, rather than
      # over the source that was meant to produce it.
      - name: Gates over the artefact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          APK=app/build/outputs/apk/debug/app-debug.apk
          V=${{ steps.version.outputs.v }}

          unzip -l "$APK" | grep -q libndi_advanced.so \
            || { echo "::error::NDI runtime missing from the APK"; exit 1; }
          unzip -l "$APK" | grep -q libndi_bridge.so \
            || { echo "::error::JNI bridge missing from the APK"; exit 1; }
          echo "PASS  the NDI runtime and the bridge are both inside"

          # Signed, and signed with the key this app has always used.
          APKSIGNER=$(ls "$ANDROID_HOME"/build-tools/*/apksigner | tail -1)
          "$APKSIGNER" verify --print-certs "$APK" > /tmp/certs.txt \
            || { echo "::error::APK is not signed"; exit 1; }
          FINGERPRINT=$(grep -m1 -oP 'SHA-256 digest: \K[0-9a-f]+' /tmp/certs.txt)

          # updatable.md §5: pin it, do not merely print it. A printed
          # fingerprint nobody compares is how a swapped key gets through, and
          # a swapped key means no build can ever replace an installed one.
          EXPECTED=$(tr -d ' \n\r' < SIGNING_FINGERPRINT.txt)
          if [ "$FINGERPRINT" != "$EXPECTED" ]; then
            echo "::error::Signing key changed. Expected $EXPECTED, got $FINGERPRINT."
            echo "::error::Every installed copy would have to be uninstalled by hand. Refusing."
            exit 1
          fi
          echo "PASS  signed with the app's own key, $FINGERPRINT"
          echo "$FINGERPRINT" > fingerprint.txt

          # The number must be greater than the last release, or the phone
          # refuses the install.
          LAST=$(gh release list --limit 100 --json tagName --jq '.[].tagName' 2>/dev/null \
            | grep -E '^v[0-9]+$' | sed 's/^v//' | sort -n | tail -1)
          if [ -n "$LAST" ] && [ "$V" -le "$LAST" ]; then
            echo "::error::appVersion is $V but v$LAST is already released. The number must rise."
            exit 1
          fi
          echo "PASS  version $V is ahead of ${LAST:-nothing}"

          # The three numbers must agree: gradle.properties, the manifest, the filename.
          AAPT=$(ls "$ANDROID_HOME"/build-tools/*/aapt2 | tail -1)
          "$AAPT" dump badging "$APK" > /tmp/badging.txt
          CODE=$(grep -oP "versionCode='\K\d+" /tmp/badging.txt)
          NAME=$(grep -oP "versionName='\K[^']+" /tmp/badging.txt)
          [ "$CODE" = "$V" ] && [ "$NAME" = "$V" ] \
            || { echo "::error::version mismatch: properties=$V code=$CODE name=$NAME"; exit 1; }
          echo "PASS  all three numbers read $V"

          # The two permissions this app cannot work without. Both have been
          # lost to a manifest merge before, and both fail silently: no
          # multicast means no receiver ever finds the source, and no
          # mediaProjection type means the service is killed on Android 14.
          grep -q "CHANGE_WIFI_MULTICAST_STATE" /tmp/badging.txt \
            || { echo "::error::multicast permission missing; no receiver would find the source"; exit 1; }
          grep -q "FOREGROUND_SERVICE_MEDIA_PROJECTION" /tmp/badging.txt \
            || { echo "::error::mediaProjection foreground type missing"; exit 1; }
          echo "PASS  the two permissions that fail silently are both present"

          # versioning.md §2: the number is written at both ends of the filename.
          cp "$APK" "$V-mantra-ndi-screen-share-v$V.apk"

      - uses: actions/upload-artifact@v4
        with:
          name: mantra-ndi-screen-share-v${{ steps.version.outputs.v }}
          path: ${{ steps.version.outputs.v }}-mantra-ndi-screen-share-v${{ steps.version.outputs.v }}.apk
          if-no-files-found: error

      # The artefact is not delivered until it is downloadable without a token.
      - name: Release
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          V=${{ steps.version.outputs.v }}
          FILE="$V-mantra-ndi-screen-share-v$V.apk"
          gh release delete "v$V" --yes --cleanup-tag 2>/dev/null || true
          gh release create "v$V" "$FILE" \
            --title "v$V" \
            --notes "Mantra NDI Screen Share v$V. Signed with the permanent key, so it updates over any earlier build carrying the same key.

          Certificate: $(cat fingerprint.txt)

          Powered by NDI. NDI is a registered trademark of Vizrt NDI AB."

          # versioning.md §4: keep the last two, remove the rest.
          gh release list --limit 100 --json tagName --jq '.[].tagName' \
            | grep -E '^v[0-9]+$' | sort -t v -k2 -n -r | tail -n +3 \
            | while read -r old; do
                echo "removing $old"
                gh release delete "$old" --yes --cleanup-tag 2>/dev/null || true
              done
